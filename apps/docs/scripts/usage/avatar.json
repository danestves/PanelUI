{
  "slug": "avatar",
  "intro": "A user's picture, falling back to initials when there is no source or the image fails to load.\n\nPass `fallback` with the initials you want; without it, a missing image leaves an empty circle.\n\nFor several people at once, `Avatar.Group` stacks them and counts the ones that did not fit.",
  "usage": "<Avatar size=\"lg\" source={{ uri: user.avatarUrl }} fallback=\"KA\" />\n\n<Avatar source={{ uri: user.avatarUrl }} fallback=\"KA\">\n  <Avatar.Badge>\n    <Badge variant=\"destructive\" count={5} />\n  </Avatar.Badge>\n</Avatar>",
  "parts": {
    "Badge": "Overlay pinned to the top-right — an unread count or a presence dot.",
    "Group": "A row of avatars, each overlapping the one after it, with the people who did not fit counted at the end."
  },
  "notes": "A plain avatar renders as one clipped node. Adding children wraps it in an unclipped container so a corner badge is not cut in half by the circular mask.\n\n`Avatar.Group` lays itself out in reverse so the first face is the one on top. Later siblings paint over earlier ones, so the stacking order and the reading order pull against each other and one of them has to be flipped; flipping the layout keeps the children in the order they were written.\n\nEach face in a group gets a ring in the page background, so the stack reads as separate people on any surface. Pass `overlap` to change how far they slide under each other — `0` closes the stack up into a plain row.",
  "anatomy": "<Avatar>\n  <Avatar.Badge>…</Avatar.Badge>\n</Avatar>\n\n<Avatar.Group>\n  <Avatar />\n</Avatar.Group>",
  "examples": [
    {
      "title": "Image with an initials fallback",
      "description": "The fallback shows while the image is missing **and** if it fails to load, so a dead URL never leaves an empty circle.",
      "code": "<Avatar source={{ uri: user.avatarUrl }} fallback=\"KA\" />\n\n{/* No source at all — straight to initials. */}\n<Avatar fallback=\"OL\" />",
      "preview": {
        "src": "/previews/avatar-with-image.jpg",
        "alt": "Avatar — Image with an initials fallback.",
        "width": 1179,
        "height": 408
      }
    },
    {
      "title": "A presence dot",
      "description": "`Avatar.Badge` hangs off an unclipped wrapper, so it is not cut in half by the avatar’s rounding.",
      "code": "<Avatar size=\"lg\" source={{ uri: user.avatarUrl }} fallback=\"KA\">\n  <Avatar.Badge>\n    <View className=\"h-3 w-3 rounded-full bg-green-500\" />\n  </Avatar.Badge>\n</Avatar>"
    },
    {
      "title": "A stack",
      "description": "`Avatar.Group` overlaps them and rings each one. Its `size` applies to every face, so the stack is set in one place rather than on each avatar.",
      "code": "<Avatar.Group size=\"sm\">\n  {members.map((member) => (\n    <Avatar\n      key={member.id}\n      source={{ uri: member.avatarUrl }}\n      fallback={member.initials}\n    />\n  ))}\n</Avatar.Group>",
      "preview": {
        "src": "/previews/avatar-stacked-group.jpg",
        "alt": "Avatar — A stack.",
        "width": 1179,
        "height": 324
      }
    },
    {
      "title": "The ones that did not fit",
      "description": "`max` caps the faces, not the row: three with five people shows three avatars and a `+2`. Pass `total` when the children are only the first few of a longer list — the count is measured against it instead of against the number of children.",
      "code": "{/* Five people, three faces, and a +2. */}\n<Avatar.Group max={3}>\n  {members.map((member) => (\n    <Avatar key={member.id} source={{ uri: member.avatarUrl }} fallback={member.initials} />\n  ))}\n</Avatar.Group>\n\n{/* Three faces out of forty — a +37. */}\n<Avatar.Group max={3} total={40}>\n  {members.slice(0, 3).map((member) => (\n    <Avatar key={member.id} source={{ uri: member.avatarUrl }} fallback={member.initials} />\n  ))}\n</Avatar.Group>"
    },
    {
      "title": "A notification badge",
      "description": "`Avatar.Badge` overlays a `Badge` in the corner — a count, or a `shape=\"dot\"` presence dot.",
      "preview": {
        "src": "/previews/avatar-with-notification-badge.jpg",
        "alt": "Avatar — A notification badge.",
        "width": 1179,
        "height": 401
      },
      "code": "<Avatar size=\"lg\" source={{ uri: user.avatar }} fallback=\"AB\">\n  <Avatar.Badge>\n    <Badge variant=\"destructive\" count={5} />\n  </Avatar.Badge>\n</Avatar>"
    }
  ],
  "variantCode": {
    "size": "<Avatar size=\"sm\" fallback=\"KA\" />\n<Avatar size=\"md\" fallback=\"KA\" />\n<Avatar size=\"lg\" fallback=\"KA\" />\n<Avatar size=\"xl\" fallback=\"KA\" />"
  },
  "preview": {
    "src": "/previews/avatar.jpg",
    "alt": "The Avatar component, running in the example app.",
    "width": 1166,
    "height": 448
  },
  "extraImports": [
    "Badge"
  ]
}
